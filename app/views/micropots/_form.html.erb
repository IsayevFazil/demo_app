<%= form_for(@micropot) do |f| %>
  <% if @micropot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micropot.errors.count, "error") %> prohibited this micropot from being saved:</h2>

      <ul>
      <% @micropot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
